# Custom Automated EDR — Python Dependencies
# Install with: pip install -r requirements.txt --break-system-packages
# On Windows in venv: pip install -r requirements.txt

# Core Windows event log access
pywin32==306          # win32evtlog, win32api, win32con, win32process

# HTTP requests for Velociraptor API + Discord webhooks
requests==2.31.0

# Web dashboard
flask==3.0.3
flask-cors==4.0.0

# Data handling
pyyaml==6.0.1         # config.yaml parsing
python-dateutil==2.9.0

# Optional: better console output
colorama==0.4.6
rich==13.7.1

# Optional: structured JSON logging
python-json-logger==2.0.7

# For process management (cross-platform fallback)
psutil==5.9.8

# Type hints support
typing-extensions==4.10.0
